/**
 * css inline basic uses 
 * css object type style use 
 * css background use 
 * css file type style use,
 * css condition type use by true false,
 * ------------------------------
 * css route active adding by class and inline style,
 * params send and catch by another component,
 * searchParam(?data="my data send") send and catch,
 * 404 route add, 
 * multiple route params adding,
 * dashboard login log out,
 * in true dashboard send false navigate to login , logout button to logout with msg data,
 * private route adding,
 * navigate with state data, useLocation,
 * difference with navigate(), Navigate,
 * useLocation full object value 
 */